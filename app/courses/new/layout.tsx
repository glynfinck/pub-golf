/** The builder page is a client component, so its tab title lives here. */
export const metadata = { title: "Plot the course" };

export default function NewCourseLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
