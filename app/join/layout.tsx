/** The join page is a client component, so its tab title lives here. */
export const metadata = { title: "Join the round" };

export default function JoinLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
